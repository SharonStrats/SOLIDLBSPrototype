#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsonld-context-parser/bin/jsonld-context-parse.js" "$@"
  ret=$?
else 
  node  "$basedir/../jsonld-context-parser/bin/jsonld-context-parse.js" "$@"
  ret=$?
fi
exit $ret
