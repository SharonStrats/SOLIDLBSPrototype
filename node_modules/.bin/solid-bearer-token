#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@solid/cli/bin/solid-bearer-token" "$@"
  ret=$?
else 
  node  "$basedir/../@solid/cli/bin/solid-bearer-token" "$@"
  ret=$?
fi
exit $ret
