import { Map } from 'immutable';
import * as C from '../../util/Consts';
import { OverloadMap, SpecialFunctionAsync } from './Types';
export interface Definition {
    arity: number | number[];
    overloads: OverloadMap;
}
export interface SpecialDefinition {
    arity: number;
    constructor: new () => SpecialFunctionAsync;
}
export declare const definitions: Map<C.Operator, Definition>;
export declare const specialDefinitions: Map<C.SpecialOperator, SpecialDefinition>;
